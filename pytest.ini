[pytest]
# The default temp root (%TEMP%\pytest-of-<user>) has broken ACLs under the
# Windows Store Python on this machine. Keep pytest scratch inside the repo;
# .pytest-tmp/ is gitignored so it can never be committed.
addopts = --basetemp=.pytest-tmp
testpaths = tests
